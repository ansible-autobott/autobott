---
# ============================================================================================================
# Goback Backup
# ============================================================================================================

- name: Main Goback block
  tags:
    - goback
  when:
    - run_role_goback is defined
    - run_role_goback
  block:
    - name: Set merged goback configuration
      ansible.builtin.set_fact:
        goback_config: "{{ goback_defaults | combine(goback, recursive=true) }}"

    - name: Check if goback is installed
      ansible.builtin.command: dpkg-query -W 'goback'
      register: goback
      failed_when: goback.rc > 1
      changed_when: goback.rc == 1

    - name: Check current goback version
      ansible.builtin.shell: "set -o pipefail && dpkg-query -W 'goback' | awk '{print $2}'| awk -F':' '{print $NF}'"
      args:
        executable: /bin/bash
      register: goback_current_version
      failed_when: false
      changed_when: false

    - name: Install goback package
      when: goback.changed or ( goback_config.version is version ( goback_current_version.stdout , '>', strict=True )  )
      ansible.builtin.apt:
        deb: "https://github.com/andresbott/goback/releases/download/v{{ goback_config.version }}/goback_{{ goback_config.version }}_linux_amd64.deb"

    - name: Ensure goback group exists
      ansible.builtin.group:
        name: "{{ goback_config.user.group }}"
        gid: "{{ goback_config.user.gid | default(omit, true) }}"

    - name: Ensure goback user exists
      ansible.builtin.user:
        name: "{{ goback_config.user.name }}"
        group: "{{ goback_config.user.group }}"
        uid: "{{ goback_config.user.uid | default(omit, true) }}"
        home: "{{ goback_config.user.home }}"
        createhome: true
        system: true

    - name: Create goback .ssh directory
      ansible.builtin.file:
        path: "{{ goback_config.user.home }}/.ssh"
        state: directory
        owner: "{{ goback_config.user.name }}"
        group: "{{ goback_config.user.group }}"
        mode: "0700"
      when: >
        (goback_config.user.ssh_key_base64 is defined and goback_config.user.ssh_key_base64 | length > 0)
        or
        (goback_config.user.ssh_pub_key is defined and goback_config.user.ssh_pub_key | length > 0)

    - name: Create SSH private key file for goback
      ansible.builtin.copy:
        content: "{{ goback_config.user.ssh_key_base64 | b64decode }}"
        dest: "{{ goback_config.user.home }}/.ssh/id_ed25519"
        owner: "{{ goback_config.user.name }}"
        group: "{{ goback_config.user.group }}"
        mode: "0600"
      no_log: true
      when:
        - goback_config.user.ssh_key_base64 is defined
        - goback_config.user.ssh_key_base64 | length > 0

    - name: Create SSH public key file for goback
      ansible.builtin.copy:
        content: "{{ goback_config.user.ssh_pub_key }}"
        dest: "{{ goback_config.user.home }}/.ssh/id_ed25519.pub"
        owner: "{{ goback_config.user.name }}"
        group: "{{ goback_config.user.group }}"
        mode: "0644"
      when:
        - goback_config.user.ssh_pub_key is defined
        - goback_config.user.ssh_pub_key | length > 0

    # ============================================================================================================
    # setup profiles
    # ============================================================================================================
    - name: Setup backup profiles
      when: run_goback_profiles
      block:
        - name: Create backup destination dirs
          ansible.builtin.include_tasks: mk_dest_dir.yaml # creates the destination dir with proper permissions
          with_items: "{{ goback_profiles }}"

        - name: Delete backup destination dir
          ansible.builtin.file:
            path: "{{ item.destination }}"
            state: absent
          with_items: "{{ goback_profiles }}"
          when:
            - not item.enabled
            - item.delete_destination is defined
            - item.delete_destination

        - name: Create backup profile
          ansible.builtin.copy:
            dest: "/etc/goback/profiles/{{ item.content.name }}.backup.yaml"
            content: "{{ item.content | to_nice_yaml(indent=2, sort_keys=False) }}"
            mode: "0644"
            validate: goback validate %s
          loop: "{{ goback_profiles }}"
          when: item.enabled

        - name: Remove backup profile
          ansible.builtin.file:
            path: "/etc/goback/profiles/{{ item.content.name }}.backup.yaml"
            state: absent
          with_items: "{{ goback_profiles }}"
          when: not item.enabled

        - name: Enable monthly backup
          ansible.builtin.file:
            src: "/etc/goback/profiles/{{ item.content.name }}.backup.yaml"
            dest: /etc/goback/backupd.monthly/{{ item.content.name }}.backup.yaml
            owner: root
            group: root
            state: link
          with_items: "{{ goback_profiles }}"
          when:
            - item.cron_monthly is defined
            - item.cron_monthly
            - item.enabled

        - name: Disable monthly backup
          ansible.builtin.file:
            path: "/etc/goback/backupd.monthly/{{ item.content.name }}.backup.yaml"
            state: absent
          with_items: "{{ goback_profiles }}"
          when: item.cron_monthly is not defined or not item.cron_monthly or not item.enabled

        - name: Enable weekly backup
          ansible.builtin.file:
            src: "/etc/goback/profiles/{{ item.content.name }}.backup.yaml"
            dest: /etc/goback/backupd.weekly/{{ item.content.name }}.backup.yaml
            owner: root
            group: root
            state: link
          with_items: "{{ goback_profiles }}"
          when:
            - item.cron_weekly is defined
            - item.cron_weekly
            - item.enabled

        - name: Disable weekly backup
          ansible.builtin.file:
            path: "/etc/goback/backupd.weekly/{{ item.content.name }}.backup.yaml"
            state: absent
          with_items: "{{ goback_profiles }}"
          when: item.cron_weekly is not defined or not item.cron_weekly or not item.enabled
