---
- name: Restart sshd
  ansible.builtin.service:
    name: "{{ item }}"
    state: restarted
  listen: restart sshd
  loop:
    - sshd
    - ssh
  failed_when: false # in case one of the services does not exist
