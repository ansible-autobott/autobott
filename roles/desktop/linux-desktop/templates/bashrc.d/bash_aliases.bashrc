# some basic aliases on desktops

alias move_videos='find . -type f \( -iname "*.mp4" -o -iname "*.mkv" -o -iname "*.avi" -o -iname "*.mov" -o -iname "*.flv" \) -exec mv -n {} ./ \;'
alias del_empty_dir='find . -type d -empty -delete'

