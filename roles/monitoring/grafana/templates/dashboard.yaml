# # config file version
apiVersion: 1

providers:
 - name: 'default'
   orgId: 1
   folder: ''
   folderUid: ''
   type: file
   allowUiUpdates: true
   options:
     path: {{ grafana_config.paths.data_dir }}/dashboards
