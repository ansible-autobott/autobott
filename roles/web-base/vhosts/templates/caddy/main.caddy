# {{ ansible_managed }}
{% for server in webservice.servers | default([]) %}
  {% if server.enabled | default(true) %}

    {% with server_index=loop.index %}
    {% include "caddy/" ~ server.type ~ ".caddy"  %}
    {% endwith %}

  {% endif %}
{% endfor %}